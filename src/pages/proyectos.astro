---
import MarketingLayout from '../layouts/MarketingLayout.astro';
import { projects } from '../lib/projects';
---

<MarketingLayout
  title="Proyectos | SOLWED - Portfolio de Diseno Web"
  description="Descubre nuestro portfolio: webs corporativas, tiendas online, integraciones ERP y mas. +50 proyectos completados."
>
  <section class="py-16 md:py-24">
    <div class="container mx-auto px-4">
      <!-- Header -->
      <div class="max-w-3xl mx-auto text-center mb-12">
        <h1 class="text-4xl md:text-5xl font-bold mb-4">
          Nuestros <span class="text-gradient">Proyectos</span>
        </h1>
        <p class="text-xl text-muted-foreground">
          +50 webs y tiendas online creadas para empresas de toda Espana.
          Cada proyecto es unico y personalizado.
        </p>
      </div>

      <!-- Projects Grid -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {projects.map((project, index) => (
          <a
            href={`https://${project.domain}`}
            target="_blank"
            rel="noopener noreferrer"
            class="group rounded-2xl border border-border bg-card overflow-hidden hover:border-primary/30 hover:shadow-lg transition-all bento-item"
            style={`animation-delay: ${index * 50}ms`}
          >
            <div class="aspect-video bg-muted relative overflow-hidden">
              <img
                src={project.image}
                alt={project.title}
                class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
                loading="lazy"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-background/80 to-transparent opacity-0 group-hover:opacity-100 transition-opacity" />
            </div>
            <div class="p-4">
              <h3 class="font-semibold mb-1">{project.title}</h3>
              <p class="text-sm text-muted-foreground flex items-center gap-1">
                <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <circle cx="12" cy="12" r="10" /><line x1="2" y1="12" x2="22" y2="12" /><path d="M12 2a15.3 15.3 0 014 10 15.3 15.3 0 01-4 10 15.3 15.3 0 01-4-10 15.3 15.3 0 014-10z" />
                </svg>
                {project.domain}
              </p>
            </div>
          </a>
        ))}
      </div>

      <!-- CTA -->
      <div class="mt-16 text-center">
        <div class="max-w-2xl mx-auto p-8 rounded-3xl border border-border bg-card">
          <h2 class="text-2xl font-bold mb-4">
            Quieres un proyecto como estos?
          </h2>
          <p class="text-muted-foreground mb-6">
            Cuentanos tu idea y te ayudamos a hacerla realidad.
            Presupuesto sin compromiso.
          </p>
          <a
            href="/contacto"
            class="inline-flex items-center justify-center h-12 px-8 font-medium rounded-md bg-primary text-primary-foreground hover:bg-primary/80 transition-all"
          >
            Solicitar presupuesto
          </a>
        </div>
      </div>
    </div>
  </section>
</MarketingLayout>
